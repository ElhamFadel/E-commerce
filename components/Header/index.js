import React from 'react'
import {Layout} from 'antd'
function Header() {
    const {Header} = Layout
    return (
        <Header>
           Header 
        </Header>
    )
}

export default Header
